- show_modal = false if local_assigns[:show_modal].nil?

nav
  ul
    li
      h1.classic = model.model_name.plural.capitalize
  - if Rails.application.routes.url_helpers.respond_to?("new_#{model.name.downcase}_path")
    ul
      - if show_modal
        li data-controller="modal-trigger"
          = link_to "#sync",
            data: { action: "click->modal-trigger#open" }
            i.icn 📥
            | Sync
      li
        = link_to controller: "#{model.model_name.plural}", action: "new"
          i.icn 🐣
          | New
