tr.hoverable id="#{dom_id product}" data-action="click->table#goTo" data-table-url-param=product_path(product)
  td.text-center
    - if product.images.present?
      = render "preloadable_img", src: product_thumb_url(product), class_names: "zoomable", width: 100
    - else
      i.icn 🧌
  td
    = product.full_title
    - if product.variations.present?
      ul.product-variations
        - product.variations.each do |variation|
          li = variation.title
  td = product.woo_id
  - if action_name != "show" 
    td.actions
      = link_to edit_product_path(product), data: {turbo: false}
        i.icn ✏️
        | Edit
