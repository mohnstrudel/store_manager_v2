= render "_shared/notice"

= render "_shared/nav-index", model: @sales

main.wide
  = paginate @sales
  table data-controller="table"
    thead
      tr
        th Woo ID
        th Customer
        th Date
        th Status
        th.right Price
        th.right Actions
    tbody
      = render @sales
  = paginate @sales
