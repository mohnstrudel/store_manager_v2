tr id="#{dom_id version}"
  td = version.id
  td = version.value
  td = format_time version.created_at
  td = format_time version.updated_at
  - if action_name != "show" 
    td.actions
      = link_to version
        i.icn 📄
        | Show
      = render "_shared/action-edit", route: edit_version_path(version)
